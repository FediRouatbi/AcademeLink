import { Settings } from '@/components/pages/settings/Settings';
import React from 'react';

const page = () => {
  return (
    <section>
      <Settings />
    </section>
  );
};

export default page;
